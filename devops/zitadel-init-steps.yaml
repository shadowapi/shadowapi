FirstInstance:
  InstanceName: ShadowAPI
  DefaultLanguage: en
  MachineKeyPath: "/secrets/shadowapi-admin-service.json"
  PatPath: "/secrets/shadowapi-admin-service.pat"
  Org:
    Name: ShadowAPI
    Human:
      UserName: admin
      FirstName: Admin
      LastName: User
      Email: admin@example.com
      Password: Admin123!
      PasswordChangeRequired: false
    Machine:
      Machine:
        Username: shadowapi-admin-service
        Name: "ShadowAPI Admin Service User"
      MachineKey:
        Type: 1
        ExpirationDate: "2099-01-01T00:00:00Z"
      Pat:
        ExpirationDate: "2099-01-01T00:00:00Z"
    # Note: Projects and Apps cannot be created via init steps YAML.
    # They must be created via API, Terraform, or Console UI after initialization.
    # See the setup documentation for creating the ShadowAPI project and OIDC application.
