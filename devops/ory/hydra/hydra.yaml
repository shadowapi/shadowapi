version: v2.2.0

serve:
  cookies:
    same_site_mode: Lax
    # Leading dot allows cookies across all subdomains
    domain: .localtest.me

  public:
    cors:
      enabled: true
      allowed_origins:
        - http://localtest.me
        - http://app.localtest.me
      allowed_methods:
        - POST
        - GET
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      allowed_headers:
        - Authorization
        - Content-Type
        - Accept
      exposed_headers:
        - Content-Type
      allow_credentials: true

urls:
  self:
    # Issuer URL must match the OIDC discovery endpoint
    issuer: http://oidc.localtest.me
  # Login and consent are handled by the backend on api subdomain
  login: http://api.localtest.me/api/v1/auth/login
  consent: http://api.localtest.me/api/v1/auth/consent
  logout: http://api.localtest.me/api/v1/auth/oauth2/logout
  error: http://api.localtest.me/auth/error
  post_logout_redirect: http://localtest.me/

oidc:
  subject_identifiers:
    supported_types:
      - pairwise
      - public
    pairwise:
      salt: xcF8QqDyp0uYvYap
  dynamic_client_registration:
    enabled: false

ttl:
  access_token: 1h
  refresh_token: 720h
  id_token: 1h
  auth_code: 10m
  login_consent_request: 30m

oauth2:
  expose_internal_errors: true
  hashers:
    bcrypt:
      cost: 10

log:
  level: debug
  format: text
  leak_sensitive_values: true

strategies:
  access_token: jwt
  scope: wildcard
