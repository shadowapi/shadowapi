version: v1.3.1

serve:
  public:
    base_url: http://localtest.me/auth/kratos/
    cors:
      enabled: true
      allowed_origins:
        - http://localtest.me
      allowed_methods:
        - POST
        - GET
        - PUT
        - PATCH
        - DELETE
      allowed_headers:
        - Authorization
        - Cookie
        - Content-Type
      exposed_headers:
        - Content-Type
        - Set-Cookie
  admin:
    base_url: http://kratos:4434/

selfservice:
  default_browser_return_url: http://localtest.me/
  allowed_return_urls:
    - http://localtest.me/
    - http://localtest.me/callback

  methods:
    password:
      enabled: true
    code:
      enabled: true
      config:
        lifespan: 15m
    link:
      enabled: true
    totp:
      enabled: true
      config:
        issuer: ShadowAPI
    lookup_secret:
      enabled: true
    webauthn:
      enabled: true
      config:
        passwordless: true
        rp:
          id: localtest.me
          display_name: ShadowAPI
          origins:
            - http://localtest.me

  flows:
    error:
      ui_url: http://localtest.me/auth/error

    settings:
      ui_url: http://localtest.me/auth/settings
      privileged_session_max_age: 15m
      required_aal: highest_available

    recovery:
      enabled: true
      ui_url: http://localtest.me/auth/recovery
      use: code

    verification:
      enabled: true
      ui_url: http://localtest.me/auth/verification
      use: code
      after:
        default_browser_return_url: http://localtest.me/

    logout:
      after:
        default_browser_return_url: http://localtest.me/login

    login:
      ui_url: http://localtest.me/login
      lifespan: 10m

    registration:
      ui_url: http://localtest.me/signup
      lifespan: 10m
      after:
        password:
          hooks:
            - hook: session
        code:
          hooks:
            - hook: session

log:
  level: debug
  format: text
  leak_sensitive_values: true

session:
  lifespan: 24h
  cookie:
    domain: localtest.me
    same_site: Lax

hashers:
  argon2:
    parallelism: 1
    memory: 128MB
    iterations: 2
    salt_length: 16
    key_length: 16

identity:
  default_schema_id: default
  schemas:
    - id: default
      url: file:///etc/config/identity.schema.json

courier:
  smtp:
    connection_uri: smtp://mailhog:1025/?disable_starttls=true
    from_address: noreply@shadowapi.com
    from_name: ShadowAPI

oauth2_provider:
  url: http://hydra:4445
