# spec/components/session_status.yaml
---
type: object
additionalProperties: false
properties:
  active:
    type: boolean
  uuid:
    type: string
    format: uuid
    description: UUID of the authenticated user
  reason:
    type: string
    description: why the session is inactive
required:
  - active
