# spec/components/user_session_token.yaml
# Deprecated: This component is no longer used (Zitadel removed)
---
type: object
additionalProperties: false
properties:
  session_token:
    type: string
    description: Session token (deprecated)
  zitadel_url:
    type: string
    description: Deprecated field
  expires_in:
    type: integer
    description: Token expiration time in seconds
required:
  - session_token
  - zitadel_url
  - expires_in
